<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="android:Theme.DeviceDefault.Light.DarkActionBar">
        <item name="android:actionBarStyle">@style/Actionbar</item>
    </style>

    <style name="Actionbar" parent="android:Widget.ActionBar">
        <item name="android:titleTextStyle">@style/ActionBar.Text</item>
        <item name="android:background">#d09922</item>
    </style>

    <style name="ActionBar.Text" parent="TextAppearance.AppCompat.Widget.ActionBar.Title">
        <item name="android:textColor">#ffffff</item>
        <item name="android:ellipsize">start</item>
    </style>

    <style name="Theme.server.List" parent="android:Widget.AbsListView">
        <item name="android:listSelector">@drawable/list_item_background</item>
        <item name="android:divider">@android:color/transparent</item>
        <item name="android:dividerHeight">0dp</item>
    </style>

    <style name="Theme.server.List.Files" parent="android:Widget.AbsListView">
        <item name="android:listSelector">@drawable/filelist_item_background</item>
        <item name="android:divider">@android:color/transparent</item>
        <item name="android:dividerHeight">0dp</item>
    </style>

    <style name="Theme.server.InputDialogue" parent="android:style/Theme.Dialog">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowBackground">@drawable/dialogue_background</item>
    </style>



</resources>
